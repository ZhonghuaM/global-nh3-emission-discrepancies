# Data directory

No unpublished research data are committed at the revision stage.

The permanent archive will contain only generated outputs and source tables that the authors are permitted to redistribute. Raw third-party data must be obtained from the official providers listed in `../metadata/data_sources.csv`.

The final archive will include checksums, licences, units, missing-value conventions, coordinate definitions, and a machine-readable manifest.
