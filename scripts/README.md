# Analysis scripts

The analysis scripts are being prepared for public release. Before inclusion, hard-coded local paths will be replaced with `config/paths.yml`, notebook outputs will be stripped, and the workflow will be tested in the pinned environment.

No script should redistribute third-party input data or contain credentials, private directory names, manuscript files, or editorial correspondence.
